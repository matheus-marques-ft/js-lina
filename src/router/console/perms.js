import ACLsMenus from './acls'

// AssetPermissions used to live here as its own top-level sibling; moved into
// ACLList.children (acls.js) so "Permissões de acesso" sits inside "Controle de acesso".
export default [...ACLsMenus]
